"""Arm 2 — the flattened/tabular representation.

One row per Subject over the same features the graph arm sees (modal Diagnosis
fields, aggregated Sample-type distribution, aggregated Intervention summary),
with the edges removed. Not a baseline in this project's vocabulary — it is the
control that separates "the features carry the signal" from "the structure
carries the signal".
"""
